package razie.actionables;

import razie.base.ActionContext

/** 
 * basic interface for an executable / task / activity
 * 
 * @author razvanc
 */
trait IExecutable extends razie.wf.JWFunc {
//  def japply (in:ActionContext, prevValue:AnyRef) : AnyRef = 
//     apply (in, prevValue).asInstanceOf[AnyRef]
}
